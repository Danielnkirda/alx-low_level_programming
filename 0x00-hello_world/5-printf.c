#include <stdio.h>

/**
 * main - print
 *
 * Return: Alway 0 (succesful c program)
 */                                         int main(void)                              {                                                   printf("with proper grammar, but theoutcome is a piece of art,\n");
        return (0);
}
