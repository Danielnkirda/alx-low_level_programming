#include"main.h"
#include <stdio.h>

/**
 * isupper check if character is uppercase
 * by checking input
 * Return: return 1 if input is uppercase otherwise always 0
 */

int _isupper(int c)
{
if (c >=65 && c<=90)
return (1);
return (0);
}
